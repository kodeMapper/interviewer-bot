# ML Service Environment Variables
# Copy to .env and fill in your values

# Host and port
HOST=0.0.0.0
PORT=8000

# Path to the backend/ directory (auto-resolved if not set)
# BACKEND_DIR=../backend

# Optional model overrides
# INTENT_MODEL_PATH=../backend/ml/models/saved/intent_model.pth
# EMBEDDING_MODEL=all-MiniLM-L6-v2
