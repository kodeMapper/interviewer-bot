# AI Smart Interviewer - Dependencies
# Python 3.10+ recommended

# Core ML / Deep Learning
torch>=2.2.2
numpy>=1.26.4
tensorflow>=2.16.1 
tf-keras>=2.16.0 # Critical for Keras 3 compatibility with Transformers

# NLP / Embeddings
sentence-transformers>=2.6.0
transformers>=4.40.0
huggingface_hub>=0.22.0

# Data Processing
scikit-learn>=1.4.0

# Visualization
matplotlib>=3.8.3

# Audio Processing (for voice interview feature)
sounddevice>=0.5.0
openai-whisper>=20231117

# Windows TTS (Windows only)
pywin32>=306; sys_platform == 'win32'

# ===== Phase 2.75: Resume Upload Feature =====
# Document Processing
PyMuPDF>=1.23.0          # PDF text extraction (fitz)
python-docx>=0.8.11      # DOCX file parsing

# LLM API Integration (Google Gemini - FREE tier available!)
google-generativeai>=0.3.0  # Gemini API client
python-dotenv>=1.0.0        # Environment variable loading (.env)

# Optional: OpenAI (if you have paid API access)
# openai>=1.12.0           # GPT API client (paid)

# Optional: OCR fallback for scanned PDFs
# pytesseract>=0.3.10
# Pillow>=10.0.0
