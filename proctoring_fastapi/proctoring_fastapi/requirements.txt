# SecureProctor AI dependencies
# Install: pip install -r requirements.txt

# Main API backend (server.py)
fastapi>=0.115,<1
uvicorn>=0.30,<1
pydantic>=2.7,<3

# Legacy backend (app.py)
Flask>=3.0,<4
flask-cors>=4.0,<5

# Vision + ML
opencv-python>=4.9,<5
torch>=2.2
numpy>=1.26,<2.0
mediapipe>=0.10,<0.11
ultralytics>=8.2,<9

# NLP / Semantic scoring (backend/core, backend/ml)
sentence-transformers>=3.0
scikit-learn>=1.4

# Resume processing (backend/resume)
python-dotenv>=1.0
python-docx>=1.1
PyMuPDF>=1.24
google-genai>=0.5
google-generativeai>=0.8

# Optional OCR fallback for scanned PDFs (backend/resume/extractor.py)
pdf2image>=1.17
pytesseract>=0.3

# Training / analysis utilities
matplotlib>=3.8

# Legacy interview controller audio stack (Windows)
sounddevice>=0.4
faster-whisper>=1.1
pywin32>=306; platform_system == "Windows"
